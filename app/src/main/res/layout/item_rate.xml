<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="@dimen/recycler_view_item_height"
    android:background="@color/cardview_light_background"
    android:layout_margin="@dimen/recycler_view_item_margin">

    <TextView
        android:id="@+id/tv_currency"
        android:layout_width="@dimen/recycler_view_currency_item_width"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:ellipsize="marquee"
        android:padding="@dimen/recycler_view_currency_item_padding"
        android:singleLine="true"
        android:textSize="@dimen/recycler_view_text_size" />

    <TextView
        android:id="@+id/tv_rate"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center_vertical" />

</LinearLayout>
